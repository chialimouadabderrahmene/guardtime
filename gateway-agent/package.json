{
  "name": "guardtime-gateway-agent",
  "version": "1.1.0",
  "description": "Linux gateway-level enforcement agent for GuardTime parental control",
  "private": true,
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "check": "for f in src/*.js; do node --check \"$f\" || exit 1; done",
    "test": "jest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.spec.js"]
  }
}
