PORT=4100
ADAPTER_TOKEN=change-me-isp-adapter-token
DEFAULT_TENANT_ID=demo-isp

# Simulation mode logs BNG actions instead of calling vendor routers.
SIMULATION_MODE=true

# Example logical BNG name used when a mock RADIUS session has no BNG.
DEFAULT_BNG_ID=bng-paris-01
